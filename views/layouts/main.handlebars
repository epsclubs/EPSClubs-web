<!doctype html>
<html>
  <head>
    <meta charSet="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0" />
    <title>{{title}}</title>

    <link href="/css/styles.css" rel="stylesheet" />
  </head>
  <body>
      <div id="topBar"  class="{{isHidden noTopBar}}">{{>topbar}}</div>
      <!-- TODO: Sidebar Hidden + js -->
      <div id="sideBar" class="{{isHidden noSideBar}}">{{>sidebar}}</div>

    {{{body}}}
  </body>
</html>
